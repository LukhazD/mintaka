version: '3.8'

services:
  mintaka:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mintaka
    restart: unless-stopped
    expose:
      - "3055"
    environment:
      - NODE_ENV=production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge